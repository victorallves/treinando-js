const nome = "victor".split("")
console.log(nome)

let repetidas = [];

let duplicata = nome.filter(function(elemento, i){
  if(nome.indexOf(elemento) !== i){
    repetidas.push(elemento)
  }
  return nome.indexOf(elemento) ==i;
})

console.log(repetidas)
